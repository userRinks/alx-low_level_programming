#include "main.h"

/**
 * _strncpy - copies string values and concatenates them
 * @dest: pointer
 * @src: pointer
 * @n: int
 * Return: strncpy
 */

char *_strncpy(char *dest, char *src, int n)
{
	return (strncpy(dest, src, n));
}
