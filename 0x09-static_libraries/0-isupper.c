#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c:	integer type number
 * Return:	(1) if uppercase character
 *		(0) if else
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
