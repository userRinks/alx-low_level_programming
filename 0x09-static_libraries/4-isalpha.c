#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isalpha - check if character is alphanumeric
 * @c: variable
 * Return: isalpha
 */

int _isalpha(int c)
{
	return (isalpha(c));
}
