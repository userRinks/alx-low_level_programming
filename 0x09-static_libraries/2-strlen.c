#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strlen - print length of string
 * @s: string
 * Return: strlen
 */

int _strlen(char *s)
{
	return (strlen(s));
}
