#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isdigit -  check if char is numeric
 * @c: integer
 * Return: 0
 */

int _isdigit(int c)

{
	return (isdigit(c));
}
