#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strcpy - copy value of one string into another
 * @dest: pointer
 * @src: pointer
 * Return: strcpy
 */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
