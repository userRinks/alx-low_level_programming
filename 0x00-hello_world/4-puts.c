/*This simple C program contains:
 * #include - "copy contents of commands from"
 * <stdio.h> - the standard C input/output header file
 * main - main block
 * puts - adds a newline character to the end of the specified argument and publishes it to the output stream
 * return (0) - end program with a value of 0 if successful
 * */


#include <stdio.h>
int main(void)

{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
